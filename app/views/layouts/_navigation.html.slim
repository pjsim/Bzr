nav.top-bar
  ul.title-area
    li.name
      h1=link_to 'Hazaar', root_path, :class => 'brand'

  section.top-bar-section
    ul.left
      li.divider
      li=link_to 'Buy', buy_index_path
      li.divider
      li=link_to 'Sell', new_product_path
      -if user_signed_in?
        li.divider
        li=link_to 'My Products', received_offers_index_path
        li.divider
        li=link_to 'My Offers', given_offers_index_path
        li.divider
        li=link_to 'My Messages', root_path
        li.divider
    ul.right
      -if user_signed_in?
        li.divider
        li=link_to "Welcome #{current_user.email}"
        li.divider
        li=link_to 'Edit Account', edit_user_registration_path
        li.divider
        li=link_to 'Logout', destroy_user_session_path, :method=>'delete'
      -else
        li.divider
        li=link_to 'Login', new_user_session_path
        li.divider
        li=link_to 'Sign Up', new_user_registration_path